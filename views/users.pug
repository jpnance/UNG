extends layout.pug

block title
	+title('All Users')

block content
	a.btn.btn-primary.mb-3(href='/users/add') Add User

	ul
		each user in users
			li
				a(href='/users/edit/'+user.username) #{user.username}
		
